<UserControl
  xmlns="https://github.com/avaloniaui"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:cv="using:MissionPlannerAvalonia.ViewModels.GCSViews.ConfigurationView"
  mc:Ignorable="d"
  d:DesignWidth="800"
  d:DesignHeight="640"
  x:Class="MissionPlannerAvalonia.Views.GCSViews.ConfigurationView.ConfigFriendlyParamsView"
  x:DataType="cv:ConfigFriendlyParamsViewModel"
  Background="#434445"
>
  <UserControl.Styles>
    <Style Selector="Border.oor">
      <Setter Property="BorderThickness" Value="2" />
      <Setter Property="BorderBrush" Value="#E64A4A" />
    </Style>
    <Style Selector="ToggleButton.star">
      <Setter Property="Content" Value="&#x2606;" />
      <Setter Property="Foreground" Value="#888888" />
      <Setter Property="Padding" Value="4,0" />
      <Setter Property="MinWidth" Value="28" />
    </Style>
    <Style Selector="ToggleButton.star:checked">
      <Setter Property="Content" Value="&#x2605;" />
      <Setter Property="Foreground" Value="#F5C518" />
    </Style>
  </UserControl.Styles>

  <Grid RowDefinitions="Auto,Auto,*" Margin="16">
    <TextBlock Grid.Row="0" Classes="pagetitle" Text="{Binding Title}" />

    <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8" Margin="0,0,0,10">
      <Button Content="Refresh Params" Command="{Binding RefreshCommand}" />
      <TextBox
        Width="240"
        Watermark="Search by name or label…"
        Text="{Binding Search}"
      />
      <TextBlock
        Text="{Binding Intro}"
        Classes="fieldlbl"
        TextWrapping="Wrap"
        VerticalAlignment="Center"
        IsVisible="{Binding Intro, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
      />
    </StackPanel>

    <ScrollViewer Grid.Row="2">
      <ItemsControl ItemsSource="{Binding Fields}">
        <ItemsControl.ItemTemplate>
          <DataTemplate x:DataType="cv:ParamField">
            <Grid ColumnDefinitions="32,220,200,Auto,*" Margin="0,3">
              <ToggleButton Grid.Column="0" Classes="star" IsChecked="{Binding Fav}" />

              <TextBlock
                Grid.Column="1"
                Text="{Binding Label}"
                Classes="fieldlbl"
                ToolTip.Tip="{Binding Description}"
                TextWrapping="Wrap"
                Margin="0,0,8,0"
              />

              <ComboBox
                Grid.Column="2"
                IsVisible="{Binding IsCombo}"
                MinWidth="180"
                ItemsSource="{Binding Options}"
                SelectedItem="{Binding SelectedOption}"
              />
              <CheckBox Grid.Column="2" IsVisible="{Binding IsBool}" IsChecked="{Binding Checked}" />

              <Button
                Grid.Column="2"
                IsVisible="{Binding IsBitmask}"
                MinWidth="180"
                HorizontalContentAlignment="Left"
                Content="{Binding BitmaskSummary}"
              >
                <Button.Flyout>
                  <Flyout>
                    <ScrollViewer MaxHeight="320">
                      <ItemsControl ItemsSource="{Binding BitOptions}">
                        <ItemsControl.ItemTemplate>
                          <DataTemplate x:DataType="cv:BitOption">
                            <CheckBox Content="{Binding Label}" IsChecked="{Binding IsSet}" />
                          </DataTemplate>
                        </ItemsControl.ItemTemplate>
                      </ItemsControl>
                    </ScrollViewer>
                  </Flyout>
                </Button.Flyout>
              </Button>

              <Border Grid.Column="2" IsVisible="{Binding IsNumeric}" Classes.oor="{Binding IsOutOfRange}">
                <NumericUpDown
                  MinWidth="180"
                  Value="{Binding Value}"
                  Minimum="{Binding Min}"
                  Maximum="{Binding Max}"
                  Increment="{Binding Increment}"
                  FormatString="0.######"
                />
              </Border>

              <TextBlock Grid.Column="3" Text="{Binding Units}" Classes="fieldlbl" Margin="8,0" />
              <TextBlock
                Grid.Column="4"
                Text="{Binding Status}"
                Foreground="#94C11F"
                VerticalAlignment="Center"
              />
            </Grid>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>
  </Grid>
</UserControl>
